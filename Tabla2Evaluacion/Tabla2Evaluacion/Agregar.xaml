<Page x:Class="Tabla2Evaluacion.Agregar"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Tabla2Evaluacion"
      mc:Ignorable="d" 
      d:DesignHeight="568" d:DesignWidth="800"
      Title="Agregar"
      Background="#242424"
      >

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Diccionario.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" ></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="70*"></RowDefinition>
            <RowDefinition Height="70*"></RowDefinition>
            <RowDefinition Height="245*"></RowDefinition>
            <RowDefinition Height="380*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Column="1">
            <Button Content="_" Width="19" Height="19" Margin="0,0,0,0" Style="{StaticResource BotonVerdeRedondo}"  Click="MinimizarVentana_Click"/>
            <Button Content="X" Width="19" Height="19" Margin="10"  Style="{StaticResource BotonVerdeRedondo}" Click="Cerrar_Click"/>
        </StackPanel>


        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center"  Margin="0,0,100,0"  Grid.Column="0" Grid.Row="1">
            <Image Source="Images/Vector (6).png" Width="30" Height="31" Margin="5" />
            <TextBlock Style="{StaticResource LetrasPeq}" VerticalAlignment="Center" Margin="15,10,35,0" Height="16" Width="92" FontSize="16"><Run Language="es-es" Text="AGREGAR"/><LineBreak/><Run Language="es-es"/></TextBlock>
        </StackPanel>


        <StackPanel Orientation="Vertical" HorizontalAlignment="Center"   Grid.Column="0" Grid.Row="2">
            <TextBlock Style="{StaticResource LetrasPeq}" FontSize="16" Margin="0,20,15,0"><Run Text="Producto "/><LineBreak/><Run/></TextBlock>
            <TextBox  x:Name="txtProducto" Style="{StaticResource LineaEscribir}" Width="178"/>
            <TextBlock Text="Categoria"  Style="{StaticResource LetrasPeq}" FontSize="16" Margin="0,15,0,0"/>
            <ComboBox x:Name="txtCategoria" Width="120" Height="23"
          FontSize="14" Foreground="White"
          HorizontalAlignment="Left" VerticalAlignment="Center"
          Margin="0,10,0,0" Background="#5DB85B" Padding="5">
              
                <ComboBox.Template>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                          
                            <Border x:Name="Border"
                Background="{TemplateBinding Background}"
                CornerRadius="5"
                BorderThickness="1"
                BorderBrush="#5DB85B"/>
                          
                            <ContentPresenter x:Name="ContentSite"
                          Content="{TemplateBinding SelectionBoxItem}"
                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                          Margin="6,3,25,3"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Left"/>
                           
                            <ToggleButton x:Name="ToggleButton"
                      Focusable="false"
                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                      ClickMode="Press"
                      HorizontalAlignment="Right"
                      Width="20"
                      Background="Transparent">
                                <Path x:Name="Arrow" 
                Fill="Black" 
                Data="M 0 0 L 4 4 L 8 0 Z" 
                Stretch="Uniform"
                Margin="0,0,5,0"/>
                            </ToggleButton>
                           
                            <Popup x:Name="Popup"
               Placement="Bottom"
               IsOpen="{TemplateBinding IsDropDownOpen}"
               AllowsTransparency="True"
               Focusable="False"
               PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" 
                  Background="Black" 
                  BorderBrush="#5DB85B" 
                  BorderThickness="1"
                  CornerRadius="5">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ComboBox.Template>

              
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Black"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Padding" Value="4,2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                      
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#333333"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ComboBox.ItemContainerStyle>

               
                <ComboBoxItem Content="Electrónica" />
                <ComboBoxItem Content="Muebles" />
                <ComboBoxItem Content="Calzado" />
            </ComboBox>







        </StackPanel>

        

            <Button Click="AgregarProducto_Click" Style="{StaticResource Botonredondomenus}" Content="Agregar" HorizontalAlignment="Left" VerticalAlignment="Bottom" Grid.Column="1" Grid.Row="2" Margin="70,0,0,30"/>
        

        <StackPanel Grid.Column="0" Grid.Row="3">
            <TextBlock Text="PRODUCTOS" Foreground="#5DB85B" FontWeight="Bold" 
   HorizontalAlignment="Center" Margin="50,0,0,5" VerticalAlignment="Top" FontSize="16"/>
            <Border BorderBrush="#5DB85B" BorderThickness="1" Height="225" Margin="80,0,0,0">
                <DataGrid x:Name="dgProductos" Background="Transparent" Margin="0,0,0,0" ></DataGrid>
            </Border>
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="3">
            <TextBlock Text="CATEGORÍA" Foreground="#5DB85B" FontWeight="Bold" 
    HorizontalAlignment="Center" Grid.Column="1" Margin="0,0,90,5" VerticalAlignment="Top" FontSize="16"/>
            <Border BorderBrush="#5DB85B" BorderThickness="1"  Grid.Column="1" Height="225" Margin="0,0,80,0" >
                <DataGrid x:Name="dgCategorias" Background="Transparent" Margin="0,0,0,0"></DataGrid>
            </Border>
        </StackPanel>

    </Grid>
</Page>
